type Props = {};
import { Link } from "react-router-dom";

const AboutPage = (props: Props) => {
  return (
    <div>
      AboutPage
      <Link to={"/"}>Main</Link>
    </div>
  );
};

export default AboutPage;
